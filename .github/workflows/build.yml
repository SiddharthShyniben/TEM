name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: @action/checkout@v2
      - uses: @action/setup-node@v2
        with: 
          node-version: 12.x
      - run: npm run build